博客标签列表
---
parameters:
  - name: page
    in: path
    type: integer
    description: 页码
    required: false
    default: 1
  - name: paginate
    in: path
    type: integer
    description: 分页，每页的数量
    required: false
    default: 10
 
responses:
  200:
    description: 返回标签列表
    schema:
      properties:
        paginate:
          type: object
          properties:
            page:
              type: string
            per_page:
              type: string
            total:
              type: string
        data:
          type: array
          items:
            properties:
              sort:
                type: integer
              create_time:
                type: string
              name:
                type: string
              update_time:
                type: string
              id:
                type: string

  400:
    description: 暂无数据
    schema:
      properties:
        msg:
          type: string

