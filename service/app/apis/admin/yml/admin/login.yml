管理员登陆
---
tags:
  - 系统管理员
parameters:
  - name: username
    in: path
    type: string
    required: true
  - name: password
    in: path
    type: string
    required: true
responses:
  200:
    description: 登陆成功
    schema:
      properties:
        msg:
          type: string
        token:
          type: string
  400:
    description: 用户名或密码错误-用户不存在
    schema:
      properties:
        msg:
          type: string
  401:
    description: 用户名或密码错误-密码错误
    schema:
      properties:
        msg:
          type: string

