删除单个博客分类信息
---
tags:
  - 系统管理员
parameters:
  - name: id
    in: path
    type: string
    description: 管理员的ID
    required: true
 
responses:
  200:
    description: 删除成功
    schema:
      properties:
        msg:
          type: string

  404:
    description: 用户不存在
    schema:
      properties:
        msg:
          type: string
  400:
    description: 删除失败
    schema:
      properties:
        msg:
          type: string

